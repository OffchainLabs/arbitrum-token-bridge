{
  "name": "arb-token-bridge-ui-mono",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start_ui": "yarn workspace arb-token-bridge-ui start",
    "start_sdk": "yarn workspace token-bridge-sdk watch",
    "start_shared_ui": "yarn workspace @arbitrum/shared-ui watch",
    "build": "yarn workspace @arbitrum/use-wallet build && yarn workspace token-bridge-sdk build && yarn workspace @arbitrum/shared-ui build && yarn workspace arb-token-bridge-ui build",
    "format": "yarn workspace arb-token-bridge-ui format && yarn workspace token-bridge-sdk format",
    "audit:ci": "audit-ci --config ./audit-ci.jsonc",
    "test:ci:sdk": "yarn workspace token-bridge-sdk test:ci",
    "test:ci:ui": "yarn workspace arb-token-bridge-ui test:ci",
    "prettier:check": "./node_modules/.bin/prettier --check .",
    "prettier:format": "./node_modules/.bin/prettier --write .",
    "serve": "serve -s packages/arb-token-bridge-ui/build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "audit-ci": "^6.3.0"
  }
}
